# dotfiles

various config files

usually these files go to `~/` (unix-like) or `%USERPROFILE%` (ms-dos), also possible per project, unless mentioned otherwise in file

paths are censored, need to modify to suitable use case
